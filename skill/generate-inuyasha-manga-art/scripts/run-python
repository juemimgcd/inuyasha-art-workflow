#!/bin/zsh
set -euo pipefail

bundled_python="/Users/jquery/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3"

if [[ -x "$bundled_python" ]]; then
  exec "$bundled_python" "$@"
fi

exec python3 "$@"
