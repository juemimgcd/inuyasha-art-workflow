# 精选原创图命名规则

使用以下结构：

```text
主体__形态__景别__内容与媒介__序号.ext
```

- 多角色用 `-` 连接，例如 `桔梗-犬夜叉`。
- 多角色形态按主体顺序用 `-` 连接，例如 `默认形态-人类形态`。
- 景别使用可检索词：`全身`、`上身`、`头部`、`双人全身`、`双人上身`、`动作`、`细节`。
- 内容写清场景、动作和媒介，例如 `星空草地拥吻彩图` 或 `花地系发带黑白漫画`。
- 同类图使用两位数字序号，从 `01` 开始。
- 相同图片可以保留在不同角色目录中；索引会按图片内容去重并合并目录标签。
